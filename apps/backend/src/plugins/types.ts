// Re-export plugin types from shared for convenience inside the backend package.
export type {
  PluginManifest,
  BackendPlugin,
  BackendPluginAPI,
  BackendConfig,
  FrontendPlugin,
} from '@midiary/shared';
